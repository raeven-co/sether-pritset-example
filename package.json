{
  "name": "sether-pritset-example",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal tokenize -> AI step -> detokenize -> Pritset PDF generation workflow using @raeven-co/sether. PII never crosses the AI trust boundary; original values are used only at document generation time.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/run.mjs"
  },
  "dependencies": {
    "@raeven-co/sether": "^0.6.0"
  }
}
